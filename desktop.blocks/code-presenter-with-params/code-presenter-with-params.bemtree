block('code-presenter-with-params').content()(function () {
    return this.doAsync(function () {
            return get('some_api_route_with_params', {tech: this.ctx.tech || 'js'});
        }.bind(this))
        .then(function (output) {
            return output.body;
        });
})
